<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Profile</title>
  <link rel="stylesheet" href="/styles/base.css">
  <link rel="stylesheet" href="/styles/profiles.css">
</head>
<body>
  <main id="new-user">
    <!-- because we send file in request, we need add enctype that controls how the form is encoded: 
      how the data will be packed into request body by the browser when the browser send the data to the server.
      in regular form (with no file): enctype="application/x-www-form-urlencoded", 
      because it is the default, we not needed to write it.
      in our case we send file, so enctype="multipart/form-data" -->
    <form action="/profiles" method="POST" enctype="multipart/form-data">    

      <div class="form-control">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" required>
      </div>
      <div class="form-control">
        <label for="image">User image</label>
        <input type="file" id="image" name="image" accept=".jpg,.jpeg,.png" >
        <!-- another option: accept='image/jpg,image/png', image/jpg includes jpeg -->
      </div>
      <button class="btn">Save User</button>
    </form>
  </main>
</body>
</html>